if ! command -v kubectl >/dev/null; then return; fi

# if [ ! -f "$KINST_LOCATION/kubectl-aliases/.kubectl_aliases" ]; then
    # git clone https://github.com/ahmetb/kubectl-aliases.git "$KINST_LOCATION/kubectl-aliases"
# fi
# source "$KINST_LOCATION/kubectl-aliases/.kubectl_aliases"

# kj() {
    # k "$@" -o json | jq
# }
# kjx() {
    # k "$@" -o json | fx
# }
# ky() {
    # k "$@" -o yaml | yh
# }
